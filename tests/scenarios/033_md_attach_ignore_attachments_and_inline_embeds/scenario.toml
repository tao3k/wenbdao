[scenario]
id = "033_md_attach_ignore_attachments_and_inline_embeds"
name = "ignore attachments and inline embeds"
description = "markdown_attachments scenario"
category = "markdown_attachments"

[input]
type = "markdown_tree"
paths = ["input"]

build_page_index = true
collect_links = true
