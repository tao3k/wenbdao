# Scenario: Quantum Fusion Contexts
# Description: Build quantum contexts from anchor hits with score fusion

[scenario]
id = "002_quantum_fusion_contexts"
name = "Quantum Fusion Contexts"
description = "Build quantum contexts from anchor hits, fusing vector and topology scores"
category = "quantum_fusion"

[input]
type = "markdown_tree"
paths = ["input/docs"]

build_page_index = true
collect_links = true
quantum_options = { alpha = 0.6, max_distance = 2, related_limit = 2 }
