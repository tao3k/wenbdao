# Alpha

alpha root section carries enough words to remain stable while powering semantic exports for hybrid retrieval.

## Beta

beta child section keeps enough detail to survive semantic document export without thinning away.

### Gamma

gamma leaf body stays visible for semantic document export tests.
