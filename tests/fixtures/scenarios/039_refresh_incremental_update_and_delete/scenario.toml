[scenario]
id = "039_refresh_incremental_update_and_delete"
name = "incremental update and delete"
description = "refresh scenario"
category = "refresh"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = true
collect_links = true
