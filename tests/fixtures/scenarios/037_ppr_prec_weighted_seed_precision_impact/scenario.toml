[scenario]
id = "037_ppr_prec_weighted_seed_precision_impact"
name = "weighted seed precision impact"
description = "ppr_precision scenario (no input)"
category = "ppr_precision"

[input]
type = "none"
paths = []

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = false
collect_links = false
