# Scenario: Page Index Headingless
# Description: Generate single root for docs without headings

[scenario]
id = "002_page_index_headingless"
name = "Page Index Headingless"
description = "Generate single root node for markdown documents without headings"
category = "page_index"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["tree.json"]

[runner]
build_page_index = true
collect_links = false
