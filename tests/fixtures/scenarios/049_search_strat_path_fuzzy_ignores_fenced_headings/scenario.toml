[scenario]
id = "049_search_strat_path_fuzzy_ignores_fenced_headings"
name = "path fuzzy ignores fenced headings"
description = "search_match_strategies scenario"
category = "search_match_strategies"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = true
collect_links = true
