[scenario]
id = "059_tree_scope_semantic_edges_restrict_section_scope"
name = "semantic edges restrict section scope"
description = "tree_scope_filters scenario"
category = "tree_scope_filters"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = true
collect_links = true
