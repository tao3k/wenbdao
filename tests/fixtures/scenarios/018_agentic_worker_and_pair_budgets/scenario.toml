[scenario]
id = "018_agentic_worker_and_pair_budgets"
name = "Agentic Expansion "
description = "Agentic expansion scenario: worker_and_pair_budgets"
category = "agentic_expansion"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = true
collect_links = true
