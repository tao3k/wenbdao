[scenario]
id = "024_cache_reuses_snapshot"
name = "Cache Build: reuses snapshot"
description = "cache_build scenario"
category = "cache_build"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = true
collect_links = true
