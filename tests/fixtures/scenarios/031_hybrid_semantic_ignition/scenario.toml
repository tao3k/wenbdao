[scenario]
id = "031_hybrid_semantic_ignition"
name = "Hybrid: semantic ignition"
description = "Hybrid quantum fusion scenario"
category = "hybrid"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["backend_error.json", "delegates_and_recovers_trace.json", "respects_min_vector_score.json", "skips_empty_requests.json"]

[runner]
build_page_index = true
collect_links = true
