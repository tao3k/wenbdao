[scenario]
id = "017_agentic_query_narrows_candidates"
name = "Agentic Expansion "
description = "Agentic expansion scenario: query_narrows_candidates"
category = "agentic_expansion"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = true
collect_links = true
