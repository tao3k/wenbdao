[scenario]
id = "013_search_core_payload_empty_graph"
name = "Search Core Empty Graph"
description = "Search with empty graph payload handling"
category = "search_core"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = true
collect_links = false
