[scenario]
id = "061_tree_scope_tree_hops_limit_section_expansion"
name = "tree hops limit section expansion"
description = "tree_scope_filters scenario"
category = "tree_scope_filters"

[input]
type = "markdown_tree"
paths = ["input"]

[expected]
type = "json_snapshot"
files = ["result.json"]

[runner]
build_page_index = true
collect_links = true
